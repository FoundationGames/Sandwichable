package io.github.foundationgames.sandwichable.util;

import net.minecraft.client.gui.AbstractParentElement;
import net.minecraft.client.gui.DrawableHelper;
import net.minecraft.client.util.math.MatrixStack;

public class RenderUtil {
    public static void renderFullnessBonusTooltip(MatrixStack matrices, AbstractParentElement parent, int x, int y, int fullness) {
        //matrices.push();

        //DrawableHelper.fill(matrices, x, y, x + 15, y + 10, 0x172d41);

        //matrices.pop();
    }
}
